---
- name: "install system software"
  apt:
    pkg: "{{ software_packages_list }}"
    state: present
    update_cache: true
    cache_valid_time: "{{ apt_cache_valid_time }}"
  tags:
    - dependent-software
